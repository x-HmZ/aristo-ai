# Aristo AI — Key Decisions Log

_Append new rows as decisions are made. Never re-litigate a decision without reading its "Why"._

| Decision | Choice | Why |
|----------|--------|-----|
| `/learn` in Pages Router | Permanent | R3F + App Router crashes on react-reconciler internals (see CLAUDE.md critical constraint) |
| FSLSM learning styles | **Removed** | Debunked; replaced by behaviorally-inferred DynamicProfile |
| Teaching model | Sonnet 4.6 | Quality on structured 5-phase output |
| Fast paths | Haiku 4.5 | explain-more, review questions, short-answer eval, free-mode |
| LLM JSON | tool_use everywhere | Guaranteed valid JSON; no regex parsing |
| TTS / STT | Web Speech (now), Whisper / TTS routes kept (planned upgrade) | Free for prototype; routes already wired for the swap |
| 3D gen | fal.ai FLUX → TripoSR (`fal-ai/triposr`, $0.07/gen) | One SDK, one key; TripoSR quality is the known weak link — see plans T06/T07 |
| Quiz UI | dedicated `QuizView`, 8 types | Replaces legacy `quiz` slice in store |
| Approval gate | `requireApproved()` placeholder | Future paywall swaps in by replacing one function body |
| Voice TTS | ElevenLabs `eleven_turbo_v2_5` via `/api/tts` | Free-tier friendly, single swap point; wawa-lipsync visemes ride the audio element |
| Desk quiz placement | `<Html transform>` at probed desk plane | Screen-anchored Html floated in mid-air; `occlude="blending"` was the original killer, not transform mode |
| Landing-page motion | `react-intersection-observer` + CSS transition, not framer-motion | Same scroll reveal for ~2 kB instead of ~38 kB on the one page whose job is to load fast (first-load JS 146 kB -> 120 kB); `prefers-reduced-motion` becomes a media query instead of a JS branch |
| Landing-page product shots | Stills captured from `/demo`, not `/learn` | Same classroom, avatar and lesson panel, but public, session-free and free to run; a real `/learn` capture costs Sonnet + fal per shot |
| Landing identity (T04b) | "Night Class": dark-first ink `#0E1117`, orange `#E98A52` (77% sat) as a spark, Archivo `wdth` 125 caps for display, light variant, follows the OS with a toggle | Hmz picked it from three canvas directions (Ember / Night Class / Cobalt). Only one where dark mode is the identity, and the warm classroom shots read as the lit window. Orange kept as the link to the product; Cobalt was rejected for splitting the brand from the classroom UI |
| Landing tokens scoped to `.landing` | Own `--lp-*` set, not the shared `--aristo-*` / shadcn tokens | `/learn` + `/demo` hardcode `#F97B2F` ~87 times, so a global change would restyle dashboard/auth but not the classroom. Product-wide migration is its own task |
| Landing display font | Archivo in `src/components/landing/fonts.ts`, not `src/lib/fonts.ts` | `src/lib/fonts.ts` is imported by both routers' roots, so anything there preloads on `/learn` and `/demo` too |
| Positioning line (V8.0, 2026-09-18) | P1: "One teacher. One student. Every kid." | Hmz picked it over P2 (method) and P3 (knowledge graph). It carries the Aristotle / one-to-one tutoring story; the five-phase section carries the method, so the headline does not have to. Bloom is cited as an idea, never as a number |
| Heading casing (V8.0, 2026-09-18) | Archivo `wdth` 125 wide caps for the hero and the closing line only; every other heading in Title Case Geist (each word capitalised) | Wide caps on every section read as shouting at parents. Title Case is Hmz's explicit choice over sentence case |
| Product name (V8.0b, 2026-09-18) | **Keep "Aristo"** | Keeping costs one session; renaming costs a sweep. Hmz chose to keep it knowing the screen's findings, recorded so nobody re-screens: aristoai.net is a live AI tutor (SAT/AP); Aristo Educational Press is a Hong Kong K-12 publisher (since 1978); aristo.com and aristo.ai are live unrelated businesses; NIVO Holding has a live EU class 41 mark and Aristo Pharma a pending EU filing covering 9 and 41; "aristo" is British slang for aristocrat. The screen (12 names, TMview US+EU cls 9/41, DNS, handles, App Store) is on the wave 1 canvas. The V8.1 rename split is cancelled; code identifiers stay |
| Landing concept (V8.0, 2026-09-18) | Pinned-scroll five-phase section (A), inside a new concept: `/` becomes a 3D animated introduction. A teacher appears, smiles and greets, then moves through interactive sections | Hmz's call; it supersedes the "looping hero video captured from /demo" plan in V8.3. Constraints V8.3 must resolve in plan mode before building: R3F cannot run under App Router (the `/learn` critical constraint), so `/` most likely moves to the Pages Router the way `/learn` did; the <=135 kB first-load JS budget and a static poster as the LCP element still apply, so the scene loads after first paint; the avatar must be the new V9 rig (<=4 MB), not the 9.6 MB Avaturn GLB; reduced motion and no-WebGL get a static page |
| Classroom UI direction (V8.0, 2026-09-18) | A: caption first. The room gets the full frame and the spoken sentence sits large in a dark glass band; the transcript moves to a drawer | Makes the one asset nobody else has the thing you look at. Open check for V8.4a: the band covers the lower third, where the desk quiz and some anchors live, so verify every camera framing with the `/dev` probes |
| Teacher look (V9.0, 2026-09-18) | L2 (stylised realistic) **and** L3 (clean stylised) both go to the V9.1 bake-off, each with L5 casting (younger, casual, peer not authority) | Hmz wants to see both built before choosing. L1 (photoreal, current) and L4 (faceless light sculpture) are rejected. Read as "bake off both"; if the intent is one L2 teacher and one L3 teacher in the cast, V9.1 should confirm before building |
| Teacher cast size (V9.0, 2026-09-18) | Two new teachers | Learners keep a choice, as with Marcus and Priya today. Every animation pack is baked for both rigs plus the Avaturn rig from `/create-teacher` |
| Marcus and Priya (V9.0, 2026-09-18) | Retired from the picker once the new pair ships; GLBs stay in the repo | The custom-teacher flow (Avaturn) is unaffected and keeps working |
| Animation strategy (V9.0, 2026-09-18) | $0 confirmed: Mixamo backbone, a director that picks clips per scenario, procedural breathing and gaze, runtime IK pointing, mirrored and time-warped variants baked in Blender | ActorCore stays a later, data-only upgrade through the animation manifest. Kimodo, Cascadeur Free and academic mocap datasets are out (hardware or licence) |
| Mark (V8.0, 2026-09-18) | **R1 The Column, with R1 extended** (both parts, as on the round-two canvas): an Ionic column built from flat bars, middle flute lit in the accent. It stands in for the I in a spaced-capitals ARISTO wordmark; the standalone column is the favicon and app tile (capital, three bars, base at 16px) | Hmz rejected M1 to M4 and supplied four references (pillar, four-point star, compass-like A, line brain); R1 comes from the pillar. The story is literal: Aristotle taught walking the Lyceum colonnade, hence "Peripatetic". Rejected: the star (reduces to the AI sparkle at 16px) and the brain (generic AI). Consequence: the wordmark moves from lowercase "aristo" to spaced capitals. The canvas sketch sets the column beside live type; V8.1 draws the whole word as one vector. Known risk: columns read as banks and law firms, which the flat drawing and lit flute are meant to offset |
| MJ's wardrobe (V9.1d, 2026-09-23) | **A: rebuild her skirt** as one continuous pleated navy A-line (`v9_skirt.py`) and repair her extended tee. **B (CC0 MPFB trousers) is the fallback** only if A cannot pass the written bar | Her extended skirt failed on modelling, not tuning: 139 panels that flare into slits, zero-area UVs stretching dark shading, a jagged hem. A keeps her look and passed the automated checks as a mock-up; B's quick fit sagged and clashed with the Canino style |
| Canino "Thinking" clip (V9.1e, 2026-09-23) | Baked from the pack's **Thinking2** (looks up, arms relaxed) and shipped under the clip name `Thinking` | The pack's Thinking puts the wrist to the chin. Copied as rotations, on the Canino proportions it lands on the chest (MJ: on her breast). A two-bone IK back to the chin either left a limp wrist or pushed Jake's skin 19 mm through his cuff. Thinking2 has no hand-to-face contact to break |
| MJ's arms vs her skirt (V9.1e, 2026-09-23) | Hanging arms swing out per frame, just enough to clear the skirt (`v9_skirt.clear_hands`): Idle 3.2-3.6 deg, Talking up to 9.9 deg, Pointing 0 | Her hands hang 10-15 mm outside her hips, so any skirt with ease swallows her fingers. A narrower skirt put her legs through it instead. Deliberate deviation from "bone directions match the source"; raised arms (Pointing's aim) are untouched |
| How the animation library ships (V9.2, 2026-09-23, Hmz) | **Base + lazy pack** per Canino teacher: `Teacher_<T>.glb` = mesh + Idle/Talking/Thinking; `Teacher_<T>_clips.glb` = every other clip, fetched after `sceneReady` (`clipPacks` in `Teacher.tsx`). Rest-value tracks are dropped and rotation keys stored as int16; the pack is meshopt | MJ paid 765 channels per clip, 0.76 MB of it JSON for tracks that never move. After the diet both rigs keep 61 per clip, and 17 clips cost less than 6 did: first load Jake 2.96 -> 2.28 MB, MJ 2.92 -> 1.78 MB. One GLB would have regressed Jake's first load. `resample` stays off: 1e-4 moved bones 0.45 deg / 7 mm, and tighter tolerances grew the pack |
| Tier 1 variants (V9.2, 2026-09-23, Hmz) | **Mirrors baked** (Talking2M, Talking3M, Talking6M, ThinkingM), made on the Mixamo source (`v9_mirror.py`) so every bake check applies. **Time-warp is runtime** (`action.timeScale`, the V9.3 director's job), not baked | A baked 0.85x/1.15x copy costs its full bytes for what `timeScale` does for free. Pointing is not mirrored: it would aim away from the board |
| Rejected pack clips (V9.2, 2026-09-23) | **Clapping** and **Talking5** are not shipped for Jake or MJ | Clapping never closes, even on Marcus: palm centres stay >= 15.7 cm apart (Jake 14.2, MJ 9.1), so it reads as a clap that misses. Talking5 is a deep wide-kneed crouch that splits MJ's skirt to the thigh (legs 70 mm outside it), and reads oddly on Jake too. `v92_clapping_contact.png`, `v92_talking5_legs.png` |
| Clip sources after V9.2 (2026-09-23, Hmz) | **No more Mixamo, no Quaternius.** Every usable Mixamo clip is already in the pack (Hmz searched it for Marcus and Priya). Quaternius UAL has one candidate (Idle Talking), which is not worth a new bone map. The gaps (wrong-answer "let us look again", present the model, thumbs up, shrug, Pointing variants) go to **Tier 2 hand-keying**, posed on `MarcusArma` and baked through the V9.2 pipeline. The Mixamo Rig add-on (IK control rig) is only worth installing for that posing: it adds no clips | Do not ask Hmz for Mixamo downloads again. ActorCore free (his login) is the only untried permitted source |
| Teacher app-scale, taller than real size (V9.2b, 2026-09-23, Hmz) | **Supersedes the flat `scale={1.5}` from the Marcus era.** Three passes: (1) true real height (Jake 1.78 m / standScale 0.9575, MJ 1.68 m / 0.9037) — Hmz called that a "dwarf" next to the old giant; (2) halfway between old scale and true height (Jake 1.2288/2.28 m, MJ 1.2019/2.23 m) — Hmz asked for 10-15% taller still; (3) **final: halfway value × 1.125** (middle of his 10-15% range): `AvatarConfig.standScale` **Jake 1.3824 (2.57 m), MJ 1.3521 (2.51 m)**, applied in `Experience.tsx` via `standScaleFor(teacher)`. Ryan/Sonia/Marcus/Priya/custom keep the old flat 1.5 (archived, never measured against furniture). `standScale * v9_export.TARGET_HEIGHT (1.859 m)` = displayed height. Feet stay planted at the same world Y (`position=[-1,-1.7,SCENE_Z]` unchanged) because the GLB's local origin is the sole of the foot, so only the scale factor moved | The classroom is built at real size (chair 0.45-0.5 m, desk 0.7-0.8 m, camera eye 1.71 m, floor y=-1.72) but both teachers stood 2.79 m — checked against the furniture for the first time this session. True real height (pass 1) read as too small once seen live; the halfway pass (2) still read short to Hmz, who asked for 10-15% taller on top of it. Final displayed height (~2.5-2.6 m) is taller than the "believable adult" bar the V9.2b brief opened with — a deliberate override by Hmz after seeing it live, not an oversight. If a future session revisits this, the true-height and halfway numbers are both recorded above — don't re-derive either |
| SCENE_Y / MODEL_Y / TEACHER_HEAD_Y re-tune (V9.2b, 2026-09-23) | Final values, tuned for the final standScale above: `SCENE_Y` (image-panel + generated-model anchor) 0.33 → **0.18**; `TEACHER_HEAD_Y` (Your-turn bubble) 0.55 → **0.65**. `spawnLabelHeight` (Thinking… label) stayed at **2.1** (local, pre-scale units) for both Jake and MJ through all three scale passes — it lives inside the teacher's own scaled group, so it tracks standScale automatically and never needed re-tuning after the first pass, unlike SCENE_Y/TEACHER_HEAD_Y which are world-space siblings of the teacher group. (Intermediate SCENE_Y/TEACHER_HEAD_Y values −0.4/0.25 and −0.035/0.4 were tuned for the two earlier scale passes — both superseded, not live) | All were tuned by eye against the 2.79 m giant, then re-tuned twice more as Hmz's requested scale changed. Verified at the final scale: Pointing's fingertip lands right at the panel's left edge for both teachers — tighter than the V9.1d bar for Marcus ("lands just outside it") — checked at the clip's exact peak-extension frame via the `window.__v92` mixer debug hook. `DeskQuiz`'s camera and paper anchor are unaffected by any of this: both are pinned to the probed student-desk geometry, not to the teacher |
| Generated-model initial spawn scale (V9.2b, 2026-09-23, Hmz) | `GeneratedModel`'s `scale` prop in `FloatingModel` (`Experience.tsx`): 1.5 → **0.825** (45% smaller, middle of Hmz's 40-50% ask) | Hmz called the initial 3D-model spawn size too big once the teacher's own scale grew. This only changes the spawn/reset size — `GeneratedModel`'s own scroll-to-resize (`onWheel`, clamped [0.4, 3.0]) is untouched, so a student can still scale it back up |
| Bent fingers fix (V9.2b, 2026-09-23) | Not a retarget bug — rest alignment and finger deltas were already exact (0.000° vs Marcus). The same Mixamo rotation values read as a tighter clench on the Canino rigs' shorter, thicker finger segments than on Marcus's. Fix: `v9_fingers.relax_fingers` slerps every finger bone's baked `rotation_quaternion` keyframe **35% toward its own rest pose**, per clip, applied after `bake_clips` (composes with `drive_helpers`/`ground`, doesn't touch the retarget maths). Applied to all 17 shipped clips for both teachers; Pointing's right index chain is excluded. Re-exported and shipped via `v9_ship.sh`; `v9_verify_anim.mjs` still passes (worst clip 0.0166°, 0.242 mm — same tolerance as V9.2, the finger relax doesn't touch drive_helpers/ground's own tracks) | Confirmed side by side in three.js (`window.__v92` mixer hook, frozen frames): at the *same* clip and time, Marcus's Talking hand is open, Jake's and MJ's were visibly clawed. 35% ("gentle" per the brief) fixed the worst offenders (Talking, to a lesser extent Idle) without flattening clips that already read fine (Talking3, Pointing) — a uniform blend was simpler and safer than hand-picking clips/frames, and its effect on already-relaxed clips is negligible. If 35% ever needs revisiting, `v9_fingers.relax_all(bpy, amount=...)` reruns the whole pass from the current (already-relaxed) keyframes, so pick a *fresh* amount relative to the untouched source, not stacked on top |
| Jake's trousers deform at the knee — fix (V9.2b, 2026-09-23, Hmz approved) | Not the normal map, not a skinning/pose bug — ruled out directly: the dark crease survived stripping the diffuse texture (still there with a plain white material, so it's real geometry, not baked-in shading) and survived setting `mixer.stopAllAction()` (zero pose applied, true bind pose), so it isn't the `KneeShareBone` helper or a weight-blend artifact either. It's a fold sculpted into the source Sketchfab trouser mesh's rest geometry itself, present regardless of pose. Fix: a distance-feathered Laplacian smooth (smoothstep falloff, factor 0.5, 3 iterations) on `Pants_14249_Shape` vertices within 0.11 m of each `CC_Base_{L,R}_Calf` bone head, edited directly on the rest mesh (`bmesh`, no armature/pose involved) — both knees, since the fold is symmetric. Re-exported and shipped; `v9_verify_anim.mjs` unaffected (animation channels untouched, only base mesh vertex positions moved) | Confirmed by elimination before touching anything, per the brief's own candidate list. Verified fixed at rest, at Talking4 (the more extreme leg pose), and at a normal (non-close-up) viewing distance, both knees. `v9_mask.find_pokes` run for trousers, shirt and shoes across all 17 clips after the edit: **0 pokes** on all three — the smoothing didn't pull the trouser surface into the leg skin anywhere. MJ's skirt/legs were checked in the same pass and show no equivalent defect (skirts don't carry this kind of sculpted knee wrinkle) — nothing to fix there |
| Animation director architecture (V9.3, 2026-09-23) | The teacher plays what `src/lib/avatar/director.ts` (pure, tested) says: one looping **base** clip, one masked **overlay**, a **look** target, a **face** hint (output only until V9.4). `Teacher.tsx` only applies the output, in `useFrame` after drei's mixer update. Clip names and scenario tags live in `animationManifest.ts`; each avatar has a clip set (`CANINO`, `AVATURN`, `CUSTOM`, `LEGACY`) | The old `useEffect` chain resolved gesture/loading/speaking with per-effect regressions (Idle clamped by a nod, skipped variants, arm pop). One pure step function makes those rules testable. Adding clips is a data change: bake, add a manifest row, add the name to a clip set |
| Pointing stays a base clip; nod and shake are head-only overlays (V9.3) | `point` is a full-body base scenario. Nod and shake are `mask: "head"` overlays over whatever the base is | Pointing's fingertip was aimed with the clip's own hips (V9.2b), so a talking base would swing it. A head-only nod leaves the body talking, which is also what makes the reaction visible in a lesson at all |
| Overlay = weight dominance on one mixer (V9.3) | An overlay is a masked copy of the clip (only the bones of `upper` or `head`) on the same mixer, weighted `w` over a base of weight 1, so it shows `w / (w + 1)` (`overlayWeight`); its eased blend is set every frame, not with `fadeIn`/`fadeOut` | three has no bone masks; `PropertyMixer` averages by weight. Linear fades through the ratio ramp far too fast and pop. The masked copy is a separate clip object so a reaction can never clamp the base action (the V9.2 frozen-Idle bug) |
| Reactions are latched events (V9.3) | The renderer subscribes to the store and latches each change of `gesture` to `nodding` or `shaking` with a counter; the director consumes the latch. The director hands the gesture back to `idle` when the reaction ends, only if the store still holds it | `submitAnswer` sets `"nodding"` and the next segment's effect sets its own gesture in the same tick, so sampling the level once a frame missed the nod in real lessons |
| ShakeNo stays for a wrong answer, for now (V9.3) | Manifest row unchanged: `wrong` plays ShakeNo. Flagged to Hmz in V9-REPORT with a recommendation to silence it until a Tier 2 "let's look again" clip exists | Keeps today's behaviour; the catalogue wants a gentler gesture, which needs a clip we do not have |
| Custom teachers keep the short clip list (V9.3) | `CUSTOM_CLIP_SET` is the seven clips they always played, so no greeting or long wait for them | Widening to `AVATURN_CLIP_SET` is one line once someone checks the Avaturn pack on a generated body |
| `lessonComplete` lives in the store (V9.3) | Non-persisted mirror of `useLessonPlayback`'s `isComplete`, set in an effect | The hook lives in the DOM tree and the teacher in the R3F canvas; no props path joins them |
| Look layer (V9.3) | Head only, applied after the mixer each frame: clamped 40 deg yaw / 25 deg pitch, weight 0.5 toward the camera and 0.7 toward board, model and desk, damped at 8/s. Targets: board and model at the scene anchor, desk at `PAPER_ANCHOR`, camera otherwise. No eyes, drift or saccades (V9.4) | The clip's own head motion is kept; a full lock reads as a mannequin. Head-only so Pointing's fingertip is not pulled off the panel |
| Bone masks by structure, tolerant of numbered names (V9.3) | `skeletonMasks` and `headBoneOf` find the head and spine by walking the chain from the hips; `HEAD` and `HIP` accept a `_<digits>` suffix | MJ's export numbers her bones (`CC_Base_Head_038`, `CC_Base_Hip_02`) and wraps them in `_scaleCompensation` nodes. The first version matched only Jake's clean names and silently disabled every overlay on MJ. Found in the running app, not by the design-time fixtures; MJ's skeleton is now a test fixture |
| Face layer is smile levels plus eyes (V9.4) | Jake and MJ have only 14 visemes, `mouthSmile` and `eyeBlinkLeft/Right`: no brow, frown or eye-look shapes. Hints: neutral 0.15, smile 1.6, warm 0.8, thinking 0.03 at rest; 0, 0.35, 0.2, 0 while speaking. Thinking also averts the eyes up and to one side | Inspected in the GLBs and confirmed on screen. `mouthSmile` moves 9 mm at full weight, so 1.0 did not read; 1.6 is a clear smile with no mesh artifacts (1.8 checked). More expressive faces need mesh work, not code |
| `smileGain` per rig (V9.4) | `AvatarConfig.morphs.smileGain`: 1 on Jake and MJ, default 0.4 (`SMILE_GAIN_UNTUNED`) for any rig not set | The smile levels are tuned on Canino only; Marcus, Priya and custom teachers share the `visemes` path and were never checked at 1.6. Set it to 1 after looking at a rig |
| Eye layer (V9.4) | Both `CC_Base_[LR]_Eye` bones, turned after the head look: 80% of the remaining error, clamp 14 deg yaw / 9 deg pitch, plus saccades (up to 3.5 / 2 deg every 0.6-2.8 s) and 1 deg drift. Rotation is built about the teacher's axes and carried into the eye's parent frame by the parent's bind orientation | The eye bones' own axes are a half-turn from the teacher's, and the clips never key them. 18 deg / 0.9 pinned the eyes at the clamp for an off-axis teacher and read as strained. Rigs with no such bones get no eye layer |
| Blink lives in `useFrame` (V9.4) | Pure scheduler in `face.ts` (1.5-5 s, 60 ms close, 120 ms open, 15% doubles) replaces the `useState` + `setTimeout` blink | The old blink re-rendered the teacher twice per blink; the new one is testable and needs no React state |
| FFT viseme fallback left as is (V9.4) | `getCurrentViseme`'s `min(1, volume * 4)` pins intensity at 1 (measured volume 0.56-0.73) | The alignment timeline is the primary path and is right; one voice is thin evidence for a new curve. Revisit if live TTS lacks character timings |
